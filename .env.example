# WC Optimizer — Environment Configuration
# Copy this file to .env and fill in your values

# ── REQUIRED FOR AI FEATURES ─────────────────────────────────────────────────
# Get your free API key at: https://console.anthropic.com
# Free tier includes $5 of free credits — enough for hundreds of queries
ANTHROPIC_API_KEY=your_key_here

# ── DATABASE (auto-configured — do not change unless you know what you're doing)
DUCKDB_PATH=./data/supply_chain.duckdb
FALKORDB_HOST=falkordb
FALKORDB_PORT=6379

# ── API SERVER
API_BASE_URL=http://api:8000
API_PORT=8000

# ── UI
STREAMLIT_PORT=8501

# ── MCP SSE SERVER (for Cursor/Claude connection)
MCP_SSE_PORT=3001
MCP_SSE_URL=http://localhost:3001/sse
